
private static final double INCREASING_MULTIPLIER = 2.0;
private static final double DECREASING_MULTIPLIER = 1.5;
//Вещественные константы класса DynArray, хранящие коэффициент увеличения и сжатия массива используются одновременно в
//нескольких местах в коде, поэтому они вынесены на класс левел и связываются на этапе компиляции. Также поэтому они
//объявлены статическими, чтобы не аллоцировать память под каждый вызов.

private String generateRandomString()
//Метод тестового класса NativeDictionaryTest предоставляет весомую гибкость в тестировании, т.к. генерирует каждый раз
//новую строку на этапе исполнения программы, что является динамическим связыванием.

var index = getIndexAsHashFun(value);
//Переменная firstIndexOfBitToSet в методе seekSlot динамически связывается во время исполнения программы, в зависимости
//от переданного в неё значения, на основе которого вычисляется хэш.