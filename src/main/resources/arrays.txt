private final String[] slots
private final T[] values
private final int[] hits
//Класс NativeCache. Можно заменить классом ArrayList<String>, ArrayList<T>, ArrayList<Integer> соответственно и получать доступ по
//индексу через api этого класса. Работа с массивом не может выполняться без прямой индексации т.к. резульат вычисления хэша
//является номером ячейки массива, куда мы кладём данные.

private final String[] slots
private final T[] values
//Класс NativeDictionary. Аналогично можно заменить тип значений на ArrayList<String> и ArrayList<T>. Работа с массивом
//не может выполняться без прямой индексации т.к. резульат вычисления хэша является номером ячейки массива, куда мы кладём данные.

private final String[] slots;
//Значения в HashTable можно также хранить в объекте типа ArrayList<String>.

--Для всех указанных значений, механизм вычисления хэша(он же индекс в массиве), предусматривает попадание в диапазон
--валидных значений.